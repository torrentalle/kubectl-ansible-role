---
# file: .travis.yml

sudo: required
language: python
cache: pip
services:
  - docker
# before_install:
#  - sudo apt-get -qq update
#  - sudo apt-get install -o Dpkg::Options::="--force-confold" --force-yes -y docker-ce
git:
  dir: kubectl
install:
  - pwd
  - ln -s  $(basename $PWD) ../kubectl
  - pip install tox-travis
env:
  matrix:
    - TOXENV=py27-ansible25
script:
  - tox
